噪声：数据里不是很好的不值得学习的特征就是噪声
           尽管那个特征很重要，但某一个数据的特征的分布特别特别大或者特别特别小的都算噪声，就叫垃圾数据或者噪声

svd降噪之后可能会产生噪点，因为有数据损失

Q:如果涉及时间序列的分析是不是必须训练集是时间序上在前的，测试集是时间序上在后的才能证明模型有效性？
A:不一定，离散的也可以

Q:数据处理的标准化和归一化处理，假如有些特别异常的极大或极小值是不是也需要处理掉
A:可以，都算作噪声

Q：KNN似乎只对简单的向量型数据有用，对图片类型有用么？
A：会把图像转换为一维数组，然后继续处理

Q：
A：